<%= form_with(model: tron_wallet) do |form| %>
  <% if tron_wallet.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tron_wallet.errors.count, "error") %> prohibited this tron_wallet from being saved:</h2>

      <ul>
        <% tron_wallet.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div>
    <%= form.label :number, style: "display: block" %>
    <%= form.number_field :number, min: 1, max: 100, value: 1, required: true %>
  </div>


  <div>
    <%= form.submit %>
  </div>
<% end %>
